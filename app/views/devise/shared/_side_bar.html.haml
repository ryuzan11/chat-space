.side-bar
  .header
    %h3.header__name
      = current_user.name

    %ul.header__lists
      %li.list
        = link_to https:// do
          %i.fa.fa-pencil-square-o.icon
      %li.list
        = link_to edit_user_path(current_user) do
          = fa_icon 'cog', class: 'icon'

  .groups
    .group
      .group__name
        グループ名
      .group__message
        作成者
